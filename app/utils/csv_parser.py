"""CSV parsing helpers placeholder."""

from typing import Iterable


def chunk_rows(_file_obj, _chunk_size: int) -> Iterable[bytes]:
    """Yield CSV data in chunks; implement streaming parser in later tasks."""
    yield from ()

